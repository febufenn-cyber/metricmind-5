{
  "name": "metricmind",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Evidence-first, multi-tenant product analytics for Postgres event data.",
  "scripts": {
    "test": "node --test",
    "test:integration": "node --test test/postgres.integration.test.js",
    "check": "node --check src/*.js && node --check test/*.js && node --check scripts/*.mjs",
    "security": "node scripts/security-check.mjs",
    "migrations:check": "node scripts/migration-check.mjs",
    "validate": "npm run check && npm run security && npm run migrations:check && npm test"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=22"
  }
}
